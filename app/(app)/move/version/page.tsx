import MoveVersionPageUI from '@/app/pages/move-version';
import { Suspense } from 'react';

export default function MoveVersionIdentifierPage({}) {
  return (
    <Suspense>
      <MoveVersionPageUI />
    </Suspense>
  );
}
